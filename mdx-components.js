import { useMDXComponents as getDocsMDXComponents } from "nextra-theme-docs";

/** Nextra reads this file by convention; it maps MDX elements to theme components. */
export const useMDXComponents = getDocsMDXComponents;
